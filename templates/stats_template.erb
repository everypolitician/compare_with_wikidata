* Items only found in SPARQL (<code>---</code> in the first column): {{{removed}}}
* Items only found in CSV (<code>+++</code> in the first column): {{{added}}}
* Items that differ between sources (&rarr; in the first column): {{{modified}}}
* Total Wikidata items returned by SPARQL query: {{{sparql_count}}}
* Total Rows in CSV: {{{csv_count}}}
